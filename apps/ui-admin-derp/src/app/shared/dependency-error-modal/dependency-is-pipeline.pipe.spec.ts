import { DependencyIsPipelinePipe } from './dependency-is-pipeline.pipe';

describe('DependencyIsPipelinePipe', () => {
  it('create an instance', () => {
    const pipe = new DependencyIsPipelinePipe();
    expect(pipe).toBeTruthy();
  });
});
