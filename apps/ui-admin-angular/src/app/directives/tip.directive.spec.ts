import { TipDirective } from './tip.directive';

describe('TipDirective', () => {
  it('should create an instance', () => {
    const directive = new TipDirective();
    expect(directive).toBeTruthy();
  });
});
