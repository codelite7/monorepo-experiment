import { ImageUtilitiesDirective } from './image-utilities.directive';

describe('ImageUtilitiesDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageUtilitiesDirective();
    expect(directive).toBeTruthy();
  });
});
